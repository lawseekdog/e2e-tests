[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    e2e: End-to-end tests
    smoke: Smoke tests
    slow: Slow running tests
    skip_seed_bootstrap: Disable external seed bootstrap for unit-style support tests
